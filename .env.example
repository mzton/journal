# Frontend env (Vite). Copy to .env.local for local dev, and set the same
# variable in your Vercel project settings for production.
#
# Base URL of the Railway backend, with NO trailing slash.
#   local:      http://localhost:8080
#   production: https://your-api.up.railway.app
VITE_API_URL=http://localhost:8080
